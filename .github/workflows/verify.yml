name: Provider Verification
on:
  workflow_dispatch:
  repository_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run the application
        run: python main.py
